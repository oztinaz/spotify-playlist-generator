import { createStore } from 'vuex'
import { State } from '@/store/types'

export default createStore<State>({
    state: {
        message: 'Hello, World!'
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
    }
})
